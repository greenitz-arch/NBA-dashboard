[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

# Cron job: runs at 2 AM ET (7 AM UTC) every day
[[scheduled-functions]]
  name = "refresh-stats"
  schedule = "0 7 * * *"
